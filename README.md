create a `config/config.env` file that has the following structure:

```bash
NODE_ENV=development
PORT=
```

```bash
npm run dev
```

```bash
http localhost:5000

# status code: 404
# "Cannot GET /"
```
